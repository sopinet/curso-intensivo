{% extends 'baseCard.html.twig' %}

{% block content %}

    <p>Esta es la carta número {{ card.id }}</p>
    <h1>{{ card.title }}</h1>
    {% if card.subtitle is not null %}
        <h2>{{ card.subtitle }}</h2>
    {% endif %}

{% endblock %}